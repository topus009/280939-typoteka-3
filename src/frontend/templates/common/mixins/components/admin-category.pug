mixin adminCategory(category)
  - itemId = `modify-form-category-${category.id}`
  li.category__list-item
    form(action='/my/categories' method='POST')
      input(type='hidden' name='_csrf' value= csrf)
      input(id=itemId type='text' name='label' value=category.label)
      input(type='hidden' name='id' value=category.id)
      label(for=itemId)
        span.visually-hidden Редактировать категорию
      button.category__button.button.button--category(type='submit') Сохранить
      button.category__button.button.button--category(type='button') Удалить
  if errors
    each error in errors
      if +error.id === +category.id
        p.comments__error= error.label
